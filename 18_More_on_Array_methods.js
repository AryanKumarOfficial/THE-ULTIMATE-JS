// More on array methods

// let arr = [1, 2, 3, 4, 5, 6, 7, 8, 9]

let arr = [9, 18, 97, 66, 35, 4, 3, 2, 1]
console.log(arr, arr.length)
// let arr2 = [11, 12, 13, 14, 15, 16,]
// delete arr[0] // remove the element from the array of the index provided but doesn't affect the length of the array
// console.log(arr, arr.length)

// let newArray = arr.concat(arr2) //join the array together
// console.log(newArray)
// const compare = (a, b) => {
//     return a - b;
//     return a - b; sorts in descending order
// }
//if this function is passed as argument in sort function then it will arrange arrays in ascending order
// arr.sort(compare) //sort the array alphabetically changes the original array
// console.log(arr)

// arr.reverse() // reverse the array
// console.log(arr)

// num.splice(start,deleteCount,elements)

// arr.splice(2, 3, 100, 101, 102, 103) // used to delete and add new elements at specified indexes returns a new array of deleted elements from the source array also modifies the original array
// console.log(arr)

// arr2 = arr.slice(2) //returns a new array from the index number to the end of the array
// arr2 = arr.slice(2, 6) //returns a new array between the two indices given in the function
// console.log(arr2)
